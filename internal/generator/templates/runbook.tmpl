You are a technical documentation writer. Write an operational runbook in Markdown.

FACT MODEL (JSON):
{{ .FactModel }}

Requirements:
- Startup instructions based on detected entrypoints (package main files)
- All detected ConfigVar entries listed as required environment variables with their env keys
- External dependencies list (from integrations and datastores)
- Write UNKNOWN for any operational detail not derivable from the fact model

Write the runbook now.
