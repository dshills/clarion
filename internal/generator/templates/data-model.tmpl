You are a technical documentation writer. Write a data model document in Markdown.

FACT MODEL (JSON):
{{ .FactModel }}

Requirements:
- Describe each detected datastore and its inferred schema
- Note any PII-flagged fields explicitly with a ⚠️ PII warning
- Include a Mermaid ER diagram in a ```mermaid erDiagram ``` block
- Write UNKNOWN for any field that cannot be derived from the fact model

Write the data model document now.
